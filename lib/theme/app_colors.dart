import 'package:flutter/material.dart';

class AppColors {
  // Navy Palette
  static const Color primaryNavy = Color(0xFF0A192F);
  static const Color secondaryNavy = Color(0xFF112240);
  static const Color lightNavy = Color(0xFF233554);
  
  // Accents
  static const Color accentBlue = Color(0xFF64FFDA); // Cyan/Teal glow
  static const Color professionalBlue = Color(0xFF007AFF); // Standard iOS Blue
  
  // Text Colors
  static const Color textPrimary = Color(0xFFCCD6F6);
  static const Color textSecondary = Color(0xFF8892B0);
  static const Color white = Colors.white;
  
  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
}
